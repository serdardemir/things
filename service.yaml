apiVersion: v1
kind: Service
metadata:
  name: test-service
  namespace: default
  labels:
    app: test-app
spec:
  selector:
    app: test-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000